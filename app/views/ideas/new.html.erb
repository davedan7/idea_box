<%= form_for(@idea) do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :description %>
  <%= f.text_field :description %>

  <%= f.collection_select(:category_id, Category.all, :id, :name) %>
  
  <%= f.submit 'Create Idea' %>
<% end %>
  